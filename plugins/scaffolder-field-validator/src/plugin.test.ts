import { scaffolderFieldValidatorPlugin } from './plugin';

describe('scaffolder-field-validator', () => {
  it('should export plugin', () => {
    expect(scaffolderFieldValidatorPlugin).toBeDefined();
  });
});
